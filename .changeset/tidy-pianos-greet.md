---
"@neovici/cosmoz-autocomplete": major
---

Select mode and design refresh: add `mode="select"` for a single-value autocomplete with unwrapped value, adopt `cosmoz-tag` for chips, migrate styles to `--cz-*` tokens from Untitled UI, and add `variant` and `hint` props
